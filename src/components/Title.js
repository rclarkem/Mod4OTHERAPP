import React, { Component } from 'react';

export class Title extends Component {
    render() {
        return (
            <div>
                <h1>Agenda App</h1>
            </div>
        );
    }
}

export default Title;
